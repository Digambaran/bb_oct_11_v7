
import React, { lazy } from 'react'
import { Suspense } from 'react'
import env from 'env'

function App() {
  return (
      <h1>Container updated develop to merge main</h1>
  )
}

export default App